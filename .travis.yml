language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gongorh/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gongorh/docker-react npm run test
